/** Copy to credentials.ts and fill in test accounts. credentials.ts is gitignored. */
export const CREDENTIALS = {
  child: { email: 'child@example.com', password: 'your-password' },
  parent: { email: 'parent@example.com', password: 'your-password' }
};
